import React, {useState} from 'react';

export default function useFilter(listToFilter) {
  const [list, setList] = useState(listToFilter);

    
        
    
    

  return [value, handleChange];
}
