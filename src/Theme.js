import { createTheme } from "@mui/material";


export const theme = createTheme({
  palette: {
    neutral: {
      main: "#64748B",
      contrastText: "#fff",
    },
    redDark: {
      main: "#c62828",
      contrastText: "#fff",
    },
  },
});
